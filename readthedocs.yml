version: 2

submodules:
  exclude: all

sphinx:
  configuration: docs/src/conf.py

build:
  image: latest

python:
  version: 3.6
  install:
  - requirements: requirements.txt
  - requirements: docs/requirements.txt
